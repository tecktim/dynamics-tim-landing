---
const base = import.meta.env.BASE_URL ?? '/';
const currentYear = new Date().getFullYear();
const legalLinks = [
  { href: `${base}impressum/`, label: 'Impressum' },
  { href: `${base}datenschutz/`, label: 'Datenschutz' }
];
---

<footer class="bg-gradient-to-br from-brand-900 via-brand-800 to-brand-700 py-12 text-white">
  <div class="mx-auto flex max-w-6xl flex-col gap-8 px-6 md:flex-row md:justify-between">
    <div class="max-w-lg">
      <p class="text-lg font-semibold text-white/90">Tim Friedrich - Dynamics 365 Freelancer</p>
      <p class="mt-2 text-sm text-white/70">
        Dynamics 365 Sales &amp; Service, Azure Integration und Power Platform - umgesetzt mit Microsoft Best Practices.
      </p>
    </div>
    <div class="flex flex-col gap-4 text-sm font-medium text-white/80 md:text-right">
      <div class="flex flex-wrap items-center gap-4 md:justify-end">
        <a href="mailto:hello@dynamics-tim.dev" class="hover:text-white">hello@dynamics-tim.dev</a>
        <a href="https://github.com/tecktim" class="hover:text-white">GitHub</a>
        <a href="https://www.linkedin.com/in/tim-friedrich-37a166203/" class="hover:text-white">LinkedIn</a>
      </div>
      <div class="flex flex-wrap items-center gap-4 md:justify-end">
        {legalLinks.map((link) => (
          <a class="hover:text-white" href={link.href}>{link.label}</a>
        ))}
        <button
          type="button"
          class="rounded px-2 py-1 text-left text-sm text-white/80 underline-offset-2 hover:text-white hover:underline"
          data-consent-manage
        >
          Einwilligung widerrufen
        </button>
      </div>
    </div>
  </div>
  <div class="mt-8 text-center text-xs text-white/60">
    &copy; {currentYear} Tim Friedrich. Alle Rechte vorbehalten.
  </div>
</footer>
