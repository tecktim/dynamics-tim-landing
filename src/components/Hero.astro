---
const base = import.meta.env.BASE_URL ?? '/';
const to = (path: string) => `${base}${path.replace(/^\//, '')}`;
---

<section class="hero-ambient relative overflow-hidden bg-gradient-to-br from-brand-900 via-brand-800 to-accent-700 py-24 text-white">
  <div class="ambient-glow pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_top,_rgba(76,162,242,0.35),_transparent_55%)]"></div>
  <div class="ambient-orb pointer-events-none absolute -right-24 top-24 h-96 w-96 rounded-full bg-[conic-gradient(from_90deg,_rgba(0,154,174,0.25),_transparent_60%)] blur-3xl"></div>
  <div class="relative mx-auto flex max-w-6xl flex-col gap-12 px-6 lg:flex-row lg:items-center">
    <div class="max-w-3xl space-y-6">
      <div class="hero-badge inline-flex items-center gap-2 rounded-full border border-white/20 bg-white/10 px-4 py-2 text-sm font-medium text-accent-100/90 shadow-soft" data-reveal data-reveal-delay="0">
        Dynamics 365 · Power Platform · Azure
      </div>
      <h1 class="font-heading text-4xl font-bold tracking-tight text-white sm:text-5xl lg:text-6xl" data-reveal data-reveal-delay="80">
        Wie verwandelt man chaotische Prozesse in selbstlaufende Systeme?
      </h1>
      <p class="text-lg text-brand-50/90 sm:text-xl" data-reveal data-reveal-delay="140">
        Ich kombiniere Power Platform Engineering, Azure Integrationen und DevOps, um komplexe Prozesse in skalierbare Anwendungen zu verwandeln.
      </p>
      <div class="flex flex-wrap gap-4" data-reveal data-reveal-delay="200">
        <a
          href="mailto:hello@dynamics-tim.dev"
          class="cta-glow rounded-2xl bg-signal-400 px-6 py-3 text-base font-semibold text-brand-900 shadow-soft transition hover:bg-signal-500 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-200"
        >
          Kostenloses Erstgespräch
        </a>
        <a
          href={to('services')}
          class="cta-outline rounded-2xl border border-white/30 bg-white/0 px-6 py-3 text-base font-semibold text-white transition hover:bg-white/10 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white/60"
        >
          Leistungen entdecken
        </a>
      </div>
      <ul class="mt-8 grid gap-3 text-base text-white/90 sm:grid-cols-3" data-reveal data-reveal-delay="260">
        {[
          'Canvas Apps & Custom Pages',
          'C# Plugins & Power Automate',
          'Azure DevOps & Observability'
        ].map((item) => (
          <li class="flex items-center gap-3 rounded-xl border border-white/15 bg-white/5 px-3 py-2 shadow-soft" key={item}>
            <span class="flex h-8 w-8 items-center justify-center rounded-full bg-white/10 text-signal-400">
              <svg viewBox="0 0 20 20" fill="currentColor" class="h-4 w-4">
                <path
                  fill-rule="evenodd"
                  d="M16.707 5.293a1 1 0 0 1 0 1.414l-7.25 7.25a1 1 0 0 1-1.414 0l-3.5-3.5a1 1 0 1 1 1.414-1.414l2.793 2.793 6.543-6.543a1 1 0 0 1 1.414 0z"
                  clip-rule="evenodd"
                ></path>
              </svg>
            </span>
            <span>{item}</span>
          </li>
        ))}
      </ul>
    </div>
    <div class="relative mx-auto w-full max-w-md" data-reveal data-reveal-delay="320">
      <div class="absolute -inset-8 rounded-3xl bg-gradient-to-br from-white/10 via-white/5 to-accent-200/20 blur-2xl"></div>
      <div class="relative rounded-3xl border border-white/20 bg-white/90 p-6 text-left text-slate-800 shadow-[0_20px_60px_rgba(0,39,76,0.25)]">
        <div class="space-y-4">
          <p class="text-sm font-semibold uppercase tracking-wide text-brand-600">ORBIS SE Referenz</p>
          <p class="text-lg text-slate-700">
            "Tim überzeugte durch seine exzellente Analysefähigkeit und fand für alle auftretenden Probleme ausnahmslos gute Lösungen."
          </p>
          <p class="text-sm font-medium text-slate-500">Microsoft Platform Solutions, ORBIS SE</p>
        </div>
      </div>
    </div>
  </div>
</section>

