---
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/Hero.astro';
import ServicesGrid from '../components/ServicesGrid.astro';
import ProjectCards from '../components/ProjectCards.astro';
import { getCollection } from 'astro:content';
import { site } from '../data/site';

const base = import.meta.env.BASE_URL ?? '/';
const to = (path: string) => {
  const trimmed = path.replace(/^\//, '');
  const [pathname, hash] = trimmed.split('#');
  const normalized = pathname ? `${pathname.replace(/\/$/, '')}/` : '';
  const suffix = hash ? `#${hash}` : '';
  return `${base}${normalized}${suffix}`;
};

const projects = (await getCollection('projects')).sort((a, b) =>
  a.data.featured === b.data.featured ? 0 : a.data.featured ? -1 : 1
);
const engagementModels = [
  {
    id: 'audit',
    badge: 'Schnelleinstieg · 1–2 Wochen',
    title: 'Audit & Strategie',
    tagline: 'Du bekommst Klarheit — nicht nur Folien.',
    description:
      'Ich analysiere deine aktuelle Dynamics 365 Landschaft: Architektur, Security, Prozesse und ALM. Am Ende steht ein priorisierter Aktionsplan mit Quick Wins, Risiken und Roadmap.',
    features: [
      'Architektur- & Security-Analyse',
      'ALM- und DevOps-Bewertung',
      'Priorisierter Maßnahmenplan',
      'Aufwandsschätzung für nächste Schritte',
    ],
    details: [
      'Security Model Audits: Business Units, Rollen, Field Security und DLP',
      'Performance Tuning für Model-driven Apps, Plugins und Datenabfragen',
      'ALM-Standards für mehrere Umgebungen und release-sichere Deployments',
    ],
    caseStudy: {
      industry: 'IT-Dienstleister',
      text: 'Für einen IT-Dienstleister habe ich in 10 Tagen eine undokumentierte Plugin-Architektur mit 40+ Custom-Steps analysiert und kritische Sicherheitslücken im Rollenmodell aufgedeckt — Ergebnis: konsoldierter Maßnahmenplan, der 3 Monate Fehlerbehebung eingespart hat.',
    },
    href: to('contact'),
    cta: 'Audit anfragen',
  },
  {
    id: 'embedded',
    badge: 'Laufend · Sprint-basiert',
    title: 'Embedded Developer',
    tagline: 'Senior-Kraft ins Team — ohne Overhead.',
    description:
      'Ich integriere mich direkt in euren SCRUM oder Kanban-Prozess und liefere Features, Plugins, Integrationen und Tests im Sprint-Rhythmus. Mit Pair-Programming und Code-Reviews hebe ich gleichzeitig das Niveau des Teams.',
    features: [
      'C# Plugins, PCF Controls & Power Automate',
      'Azure Functions & Service Bus Integrationen',
      'Playwright UI-Tests & Quality Gates',
      'Pair-Programming & Code-Reviews',
    ],
    details: [
      'Dynamics 365 Sales/Service Integrationen mit ERP, BI und Microsoft 365',
      'Power Automate Orchestrierung mit sauberem Fehlerhandling und Monitoring',
      'Azure Functions, Service Bus und Event Grid für event-driven Workflows',
    ],
    caseStudy: {
      industry: 'Maschinenbauunternehmen',
      text: 'Für ein Maschinenbauunternehmen habe ich als Embedded Developer über 6 Monate eine komplexe ERP-Dynamics-Integration mit Azure Service Bus und automatisierten UI-Tests aufgebaut — Ergebnis: stabile Release-Zyklen alle 2 Wochen ohne manuelle Regressionstests.',
    },
    href: to('contact'),
    cta: 'Verfügbarkeit anfragen',
  },
  {
    id: 'fullpackage',
    badge: 'End-to-End · 3–4 Monate',
    title: 'Full Package: Audit → Enablement',
    tagline: 'Einmal richtig machen. Dauerhaft wirken.',
    description:
      'Vom Audit über Konzeption und Umsetzung bis zur Team-Übergabe. Ich verantworte den gesamten Zyklus und stelle sicher, dass dein Team am Ende eigenständig releasen kann.',
    features: [
      'Vollständiger Audit & Zielbild',
      'Iterative Umsetzung mit DevOps',
      'Dokumentation & Übergabe-Sprint',
      'Team-Enablement & Coaching',
    ],
    details: [
      'Dataverse, Model-driven Apps, Custom Pages und Canvas Apps',
      'CI/CD mit Azure DevOps oder GitHub Actions inklusive Quality Gates',
      'Monitoring über Application Insights, Log Analytics und Alerts',
    ],
    caseStudy: {
      industry: 'Handelsunternehmen',
      text: 'Für ein Handelsunternehmen habe ich von Audit bis Übergabe ein komplettes Dynamics 365 Sales-Rollout durchgeführt — Ergebnis: das interne Team konnte nach 12 Wochen eigenständig neue Features deployen, ohne externen Support.',
    },
    href: to('contact'),
    cta: 'Gesamtpaket anfragen',
  },
];

const valueBullets = [
  {
    title: 'Embedded ins Team',
    description:
      'Ich verstärke dein bestehendes Team mit Senior-Level Expertise in Dynamics 365 Development und Consulting.',
  },
  {
    title: 'Full-Stack Power Platform',
    description:
      'Canvas Apps, React/TypeScript-PCFs, C# Plugins und Automatisierung aus einer Hand.',
  },
  {
    title: 'Release-Sicherheit inklusive',
    description:
      'Azure DevOps, Code-Quality-Gates und Monitoring sorgen für planbare Deployments ohne Überraschungen.',
  },
];

const approachSteps = [
  {
    phase: '01',
    duration: 'Woche 1–2',
    title: 'Audit',
    tagline: 'Ich bringe die Wahrheit ans Licht — auch die unbequeme.',
    description:
      'Ist-Analyse von Architektur, Security Model, Datenflüssen und ALM-Setup. Am Ende weißt du genau, wo die größten Risiken und schnellsten Wins liegen — nicht was du hören willst, sondern was du brauchst.',
  },
  {
    phase: '02',
    duration: 'Woche 2–3',
    title: 'Konzeption',
    tagline: 'Kein Slide-Deck. Ein echter Plan.',
    description:
      'Technisches Zielbild, priorisiertes Backlog und messbare Erfolgskennzahlen. Stakeholder werden früh eingebunden — damit Überraschungen in der Umsetzung die Ausnahme sind, nicht die Regel.',
  },
  {
    phase: '03',
    duration: 'Woche 3–10',
    title: 'Umsetzung',
    tagline: 'Keine Big-Bang-Releases. Kleine Increments, jedes getestet.',
    description:
      'Iterative Implementierung mit Plugins, Automationen, Integrationen — und Tests für alles Business-kritische. DevOps und Quality Gates laufen von Anfang an mit, nicht am Ende.',
  },
  {
    phase: '04',
    duration: 'Woche 9–12',
    title: 'Enablement',
    tagline: 'Ich gehe. Dein Team bleibt handlungsfähig.',
    description:
      'Dokumentation, Pairing-Sessions und Übergabe. Dein Team kann stabil releasen und die Lösung eigenständig weiterentwickeln — ohne mich rufen zu müssen.',
  },
];

const faqs = [
  {
    question: 'Was kostet die Zusammenarbeit?',
    answerHtml:
      'Ich arbeite auf Tagessatz-Basis. Je nach Scope und Engagement-Modell liegt ein typisches Projekt zwischen 5 und 25 Tagen. Im kostenlosen Erstgespräch klären wir den genauen Rahmen und ich erstelle dir eine transparente Aufwandsschätzung — ohne Überraschungen.',
    plainAnswer:
      'Ich arbeite auf Tagessatz-Basis. Je nach Scope und Engagement-Modell liegt ein typisches Projekt zwischen 5 und 25 Tagen. Im kostenlosen Erstgespräch klären wir den genauen Rahmen und ich erstelle dir eine transparente Aufwandsschätzung — ohne Überraschungen.',
  },
  {
    question: 'Wie starte ich ein Dynamics 365 Projekt mit dir als Freelancer?',
    answerHtml:
      'Wir beginnen mit einem fokussierten Erstgespräch, in dem wir die wichtigsten Dynamics 365 Use Cases priorisieren und das weitere Vorgehen klären. Über das <a href="mailto:hello@dynamics-tim.dev" class="text-brand-500 underline decoration-2 decoration-brand-200 hover:text-brand-400">Kontaktformular</a> oder per Mail erhältst du in 48 Stunden eine Roadmap inklusive Aufwandsschätzung.',
    plainAnswer:
      'Wir starten mit einem Erstgespräch, in dem wir die wichtigsten Dynamics 365 Use Cases priorisieren und das weitere Vorgehen klären. Über das Kontaktformular oder per Mail erhältst du in 48 Stunden eine Roadmap inklusive Aufwandsschätzung.',
  },
  {
    question: 'Unterstützt du auch bestehende Dynamics 365 Teams langfristig?',
    answerHtml: `Ja. Ich coache interne Entwickler:innen, führe gemeinsame Code-Reviews durch und implementiere Quality Gates sowie Automatisierung. Über den <a href="${to('profile')}" class="text-brand-500 underline decoration-2 decoration-brand-200 hover:text-brand-400">Profil-Bereich</a> findest du Referenzen und konkrete Technologien, die ich einbringe.`,
    plainAnswer:
      'Ich unterstütze bestehende Dynamics 365 Teams mit Coaching, Code-Reviews, Quality Gates und Automatisierung. Referenzen und Technologien findest du im Profil-Bereich.',
  },
  {
    question: 'Welche Dynamics 365 und Power Platform Technologien nutzt du?',
    answerHtml: `Ich arbeite mit C# Plugins, Power Automate, PCF Controls, Azure Functions, Service Bus, Event Grid und Playwright für UI Test Automation. Diese Technologien ermöglichen es mir, skalierbare und effiziente Lösungen zu entwickeln, die den spezifischen Anforderungen von Dynamics 365 gerecht werden.`,
    plainAnswer:
      'Ich arbeite mit C# Plugins, Power Automate, PCF Controls, Azure Functions, Service Bus, Event Grid und Playwright für UI Test Automation. Diese Technologien ermöglichen es mir, skalierbare und effiziente Lösungen zu entwickeln, die den spezifischen Anforderungen von Dynamics 365 gerecht werden.',
  },
  {
    question: 'Wie stellst du Security und Performance in Dynamics 365 sicher?',
    answerHtml:
      'Ich prüfe Security Model, Rollen, Field Security und DLP-Regeln und optimiere Plugins, Abfragen und Client-Skripte. Dazu kommen Monitoring, Audit-Logs und klare Performance-Guidelines.',
    plainAnswer:
      'Ich prüfe Security Model, Rollen, Field Security und DLP-Regeln und optimiere Plugins, Abfragen und Client-Skripte. Dazu kommen Monitoring, Audit-Logs und klare Performance-Guidelines.',
  },
  {
    question: 'Welche Deliverables bekomme ich nach dem Projektstart?',
    answerHtml:
      'Du erhältst ein priorisiertes Backlog, ein technisches Zielbild, Empfehlungen für ALM/CI-CD sowie eine Roadmap für die nächsten Sprints. Bei Bedarf ergänze ich eine Referenzarchitektur und ein Enablement-Konzept.',
    plainAnswer:
      'Du erhältst ein priorisiertes Backlog, ein technisches Zielbild, Empfehlungen für ALM/CI-CD sowie eine Roadmap für die nächsten Sprints. Bei Bedarf ergänze ich eine Referenzarchitektur und ein Enablement-Konzept.',
  },
];

const canonicalUrl = Astro.site
  ? new URL(Astro.url.pathname, Astro.site).toString()
  : Astro.url.href;
const siteUrl = Astro.site ? Astro.site.toString() : new URL('/', canonicalUrl).toString();
const providerId = `${siteUrl}#person`;

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((faq) => ({
    '@type': 'Question',
    name: faq.question,
    acceptedAnswer: {
      '@type': 'Answer',
      text: faq.plainAnswer,
    },
  })),
};

const serviceItemList = {
  '@context': 'https://schema.org',
  '@type': 'ItemList',
  name: 'Dynamics 365 & Power Platform Services',
  itemListElement: engagementModels.map((service, index) => {
    const serviceUrl = new URL(service.href ?? '/', siteUrl).toString();
    return {
      '@type': 'ListItem',
      position: index + 1,
      item: {
        '@type': 'Service',
        name: service.title,
        description: service.description,
        url: serviceUrl,
        provider: {
          '@type': 'Person',
          '@id': providerId,
        },
        areaServed: {
          '@type': 'Country',
          name: 'Deutschland',
        },
      },
    };
  }),
};

const structuredData = [faqSchema, serviceItemList];
---

<BaseLayout
  title="Dynamics 365 Developer & Technical Consultant"
  description="Dynamics 365 Freelancer für Service- & Sales-Prozesse: Power Platform Engineering, Azure Integrationen, DevOps & Enablement."
  breadcrumbs={[{ name: "Startseite", url: "/" }]}
>
  <Hero />

  {/* Problem → Lösung → Ergebnis strip */}
  <section class="bg-white py-16 border-b border-surface-100 animate-on-scroll">
    <div class="mx-auto max-w-6xl px-6">
      <div class="grid gap-8 md:grid-cols-3">
        <div
          class="rounded-2xl border border-red-100 bg-red-50/60 p-6 space-y-3"
          data-reveal
          data-reveal-delay="0"
        >
          <div
            class="inline-flex items-center gap-2 rounded-full bg-red-100 px-3 py-1 text-xs font-semibold uppercase tracking-wide text-red-700"
          >
            Euer Status Quo
          </div>
          <ul class="space-y-2 text-sm text-slate-700">
            <li class="flex items-start gap-2">
              <span class="mt-0.5 text-red-400">✗</span> Fragile Plugins, die bei jedem Update brechen
            </li>
            <li class="flex items-start gap-2">
              <span class="mt-0.5 text-red-400">✗</span> Manuelle Prozesse, die niemand gerne macht
            </li>
            <li class="flex items-start gap-2">
              <span class="mt-0.5 text-red-400">✗</span> Releases, die alle nervös machen
            </li>
            <li class="flex items-start gap-2">
              <span class="mt-0.5 text-red-400">✗</span> Kein Team, das die Lösung eigenständig pflegen
              kann
            </li>
          </ul>
        </div>
        <div
          class="rounded-2xl border border-brand-100 bg-brand-50/60 p-6 space-y-3"
          data-reveal
          data-reveal-delay="80"
        >
          <div
            class="inline-flex items-center gap-2 rounded-full bg-brand-100 px-3 py-1 text-xs font-semibold uppercase tracking-wide text-brand-700"
          >
            Was ich ändere
          </div>
          <ul class="space-y-2 text-sm text-slate-700">
            <li class="flex items-start gap-2">
              <span class="mt-0.5 text-brand-400">→</span> Saubere Plugin-Architektur mit Testabdeckung
            </li>
            <li class="flex items-start gap-2">
              <span class="mt-0.5 text-brand-400">→</span> Automatisierte Workflows mit robustem Fehlerhandling
            </li>
            <li class="flex items-start gap-2">
              <span class="mt-0.5 text-brand-400">→</span> CI/CD-Pipelines mit Quality Gates
            </li>
            <li class="flex items-start gap-2">
              <span class="mt-0.5 text-brand-400">→</span> Enablement, das Wissen im Team hält
            </li>
          </ul>
        </div>
        <div
          class="rounded-2xl border border-green-100 bg-green-50/60 p-6 space-y-3"
          data-reveal
          data-reveal-delay="160"
        >
          <div
            class="inline-flex items-center gap-2 rounded-full bg-green-100 px-3 py-1 text-xs font-semibold uppercase tracking-wide text-green-700"
          >
            Euer Ergebnis
          </div>
          <ul class="space-y-2 text-sm text-slate-700">
            <li class="flex items-start gap-2">
              <span class="mt-0.5 text-green-500">✓</span> Schnellere Release-Zyklen ohne Überraschungen
            </li>
            <li class="flex items-start gap-2">
              <span class="mt-0.5 text-green-500">✓</span> Confidence bei jedem Deployment
            </li>
            <li class="flex items-start gap-2">
              <span class="mt-0.5 text-green-500">✓</span> Ein Team, das selbstständig weiterentwickelt
            </li>
            <li class="flex items-start gap-2">
              <span class="mt-0.5 text-green-500">✓</span> CRM, das Vertrieb und Service wirklich nutzen
            </li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <section class="bg-surface-100 py-20 animate-on-scroll">
    <div class="mx-auto max-w-6xl px-6">
      <h2 class="section-title" data-reveal data-reveal-delay="0">Warum Tim?</h2>
      <div class="mt-10 grid gap-6 md:grid-cols-3" data-reveal data-reveal-delay="80">
        {
          valueBullets.map((bullet, index) => (
            <div class="card" data-reveal data-reveal-delay={`${index * 80 + 160}`}>
              <h3 class="text-xl font-semibold text-slate-900">{bullet.title}</h3>
              <p class="mt-3 text-base text-slate-600">{bullet.description}</p>
            </div>
          ))
        }
      </div>
      <div
        class="mt-12 rounded-2xl border border-red-100 bg-red-50/40 p-6 md:p-8"
        data-reveal
        data-reveal-delay="400"
      >
        <h3 class="text-lg font-semibold text-slate-900 mb-4">
          Was ich <span class="text-red-600">NICHT</span> mache
        </h3>
        <div class="grid gap-4 md:grid-cols-3">
          <div class="flex items-start gap-3">
            <span
              class="mt-0.5 flex h-6 w-6 shrink-0 items-center justify-center rounded-full bg-red-100 text-red-500 text-sm font-bold"
              >✗</span
            >
            <p class="text-sm text-slate-600">
              <strong class="text-slate-800">Kein 50-Personen-Rollout.</strong> Ich bin Freelancer, kein
              Systemhaus. Mein Sweet Spot: gezielte Verstärkung für Teams von 2–15.
            </p>
          </div>
          <div class="flex items-start gap-3">
            <span
              class="mt-0.5 flex h-6 w-6 shrink-0 items-center justify-center rounded-full bg-red-100 text-red-500 text-sm font-bold"
              >✗</span
            >
            <p class="text-sm text-slate-600">
              <strong class="text-slate-800">Keine Slide-Decks ohne Code.</strong> Ich liefere funktionierende
              Lösungen, nicht Management-Präsentationen die in der Schublade landen.
            </p>
          </div>
          <div class="flex items-start gap-3">
            <span
              class="mt-0.5 flex h-6 w-6 shrink-0 items-center justify-center rounded-full bg-red-100 text-red-500 text-sm font-bold"
              >✗</span
            >
            <p class="text-sm text-slate-600">
              <strong class="text-slate-800">Kein Offshore-Team.</strong> Du arbeitest direkt mit mir
              — DACH-Zeitzonen, deutsche Kommunikation, persönliche Verantwortung.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="bg-surface-100 py-20 animate-on-scroll" id="engagement-modelle">
    <div class="mx-auto max-w-6xl px-6">
      <div class="mb-10 max-w-3xl space-y-4">
        <h2 class="section-title" data-reveal data-reveal-delay="0">So arbeiten wir zusammen</h2>
        <p class="text-lg text-slate-600" data-reveal data-reveal-delay="80">
          Drei Wege, wie ich dein Projekt voranbringen kann — je nachdem, wo du gerade stehst.
        </p>
      </div>
      <div class="grid gap-6 lg:grid-cols-3">
        {
          engagementModels.map((model, index) => (
            <article
              class="card flex flex-col gap-5 border-surface-200 bg-white/90"
              data-reveal
              data-reveal-delay={`${index * 100 + 160}`}
            >
              <div>
                <span class="inline-flex items-center rounded-full bg-brand-50 px-3 py-1 text-xs font-semibold text-brand-700 ring-1 ring-brand-200/60">
                  {model.badge}
                </span>
              </div>
              <div>
                <h3 class="text-2xl font-semibold text-slate-900">{model.title}</h3>
                <p class="mt-1 text-sm font-medium text-brand-600 italic">"{model.tagline}"</p>
              </div>
              <p class="text-base text-slate-600">{model.description}</p>
              <ul class="space-y-1.5">
                {model.features.map((feature) => (
                  <li class="flex items-center gap-2 text-sm text-slate-700">
                    <span class="flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-brand-100 text-brand-600">
                      <svg
                        viewBox="0 0 20 20"
                        fill="currentColor"
                        class="h-3 w-3"
                        aria-hidden="true"
                      >
                        <path
                          fill-rule="evenodd"
                          d="M16.707 5.293a1 1 0 0 1 0 1.414l-7.25 7.25a1 1 0 0 1-1.414 0l-3.5-3.5a1 1 0 1 1 1.414-1.414l2.793 2.793 6.543-6.543a1 1 0 0 1 1.414 0z"
                          clip-rule="evenodd"
                        />
                      </svg>
                    </span>
                    {feature}
                  </li>
                ))}
              </ul>
              {model.details && model.details.length > 0 && (
                <details class="group/det rounded-xl border border-surface-200 bg-surface-50 text-sm">
                  <summary class="flex cursor-pointer items-center justify-between gap-2 p-4 text-xs font-semibold uppercase tracking-wide text-slate-500 hover:text-brand-600 transition-colors">
                    <span>Use Cases &amp; Tech Details</span>
                    <span
                      class="text-brand-400 transition-transform group-open/det:rotate-180"
                      aria-hidden="true"
                    >
                      ▾
                    </span>
                  </summary>
                  <ul class="px-4 pb-4 space-y-1.5">
                    {model.details.map((detail: string) => (
                      <li class="flex items-start gap-2 text-slate-600">
                        <span class="mt-1 flex h-4 w-4 shrink-0 items-center justify-center rounded-full bg-brand-100 text-brand-600">
                          <svg
                            viewBox="0 0 20 20"
                            fill="currentColor"
                            class="h-2.5 w-2.5"
                            aria-hidden="true"
                          >
                            <path
                              fill-rule="evenodd"
                              d="M16.707 5.293a1 1 0 0 1 0 1.414l-7.25 7.25a1 1 0 0 1-1.414 0l-3.5-3.5a1 1 0 1 1 1.414-1.414l2.793 2.793 6.543-6.543a1 1 0 0 1 1.414 0z"
                              clip-rule="evenodd"
                            />
                          </svg>
                        </span>
                        {detail}
                      </li>
                    ))}
                  </ul>
                </details>
              )}
              <details class="group/spot rounded-xl border border-surface-200 bg-surface-50 text-sm">
                <summary class="flex cursor-pointer items-center justify-between gap-2 p-4 text-xs font-semibold uppercase tracking-wide text-slate-500 hover:text-brand-600 transition-colors">
                  <span>Projekt-Spotlight: {model.caseStudy.industry}</span>
                  <span
                    class="text-brand-400 transition-transform group-open/spot:rotate-180"
                    aria-hidden="true"
                  >
                    ▾
                  </span>
                </summary>
                <div class="px-4 pb-4">
                  <p class="text-slate-600 leading-relaxed">{model.caseStudy.text}</p>
                </div>
              </details>
              <a
                href={model.href}
                class="mt-auto inline-flex items-center gap-1 text-sm font-semibold text-brand-500 transition hover:text-brand-400"
              >
                <span>{model.cta}</span>
                <span aria-hidden="true">&rarr;</span>
              </a>
            </article>
          ))
        }
      </div>

      {/* 3-Question Selector Quiz */}
      <div
        class="mt-14 rounded-2xl border border-brand-100 bg-gradient-to-b from-brand-50/80 to-white p-8"
        data-reveal
        data-reveal-delay="460"
        id="engagement-quiz"
      >
        <div class="grid gap-5 md:grid-cols-[minmax(0,1fr)_auto] md:items-start">
          <div class="max-w-2xl">
            <p class="mb-2 text-sm font-semibold uppercase tracking-wide text-brand-700">
              Nicht sicher, was du brauchst?
            </p>
            <h3 class="text-2xl font-semibold text-slate-900">
              Beantworte 3 Fragen und erhalte eine passende Empfehlung.
            </h3>
            <p class="mt-2 text-sm text-slate-600">
              Dauer: unter 60 Sekunden. Du kannst deine Antworten jederzeit anpassen.
            </p>
          </div>
          <div
            class="rounded-xl border border-brand-100 bg-white/90 px-4 py-3 text-xs text-slate-600"
          >
            <p class="font-semibold uppercase tracking-wide text-brand-700">Schnellcheck</p>
            <p class="mt-1">Direkt umsetzbare Empfehlung statt allgemeiner Sales-Floskeln.</p>
          </div>
        </div>

        <div class="mt-7 space-y-5" id="quiz-container">
          <div class="rounded-2xl border border-surface-200 bg-white p-5">
            <div class="mb-3 flex items-center justify-between gap-3">
              <p id="quiz-step-label" class="text-sm font-semibold text-slate-800">Frage 1 von 3</p>
              <button
                id="quiz-reset-top"
                type="button"
                class="hidden rounded-lg border border-surface-300 px-3 py-1.5 text-xs font-medium text-slate-600 transition hover:border-brand-300 hover:text-brand-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-300/60"
              >
                Neu starten
              </button>
            </div>
            <div class="h-2 overflow-hidden rounded-full bg-surface-100">
              <div
                id="quiz-progress-bar"
                class="h-full w-0 rounded-full bg-gradient-to-r from-brand-500 to-accent-500 transition-all duration-300"
              >
              </div>
            </div>
          </div>

          <div class="space-y-5" id="quiz-steps">
            <div class="quiz-step rounded-2xl border border-surface-200 bg-white p-6" data-step="1">
              <p class="text-base font-medium text-slate-800">
                1. Wie gut kennst du deinen aktuellen D365-Zustand?
              </p>
              <div class="mt-4 grid gap-3 sm:grid-cols-2">
                <button
                  type="button"
                  class="quiz-btn rounded-xl border border-slate-200 bg-white px-4 py-3 text-left text-sm font-medium text-slate-700 transition hover:border-brand-400 hover:text-brand-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-300/60"
                  data-q="1"
                  data-v="unknown"
                  aria-pressed="false"
                >
                  Kaum — wir haben kaum Dokumentation
                </button>
                <button
                  type="button"
                  class="quiz-btn rounded-xl border border-slate-200 bg-white px-4 py-3 text-left text-sm font-medium text-slate-700 transition hover:border-brand-400 hover:text-brand-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-300/60"
                  data-q="1"
                  data-v="partial"
                  aria-pressed="false"
                >
                  Teilweise - wir wissen, wo es hakt
                </button>
                <button
                  type="button"
                  class="quiz-btn rounded-xl border border-slate-200 bg-white px-4 py-3 text-left text-sm font-medium text-slate-700 transition hover:border-brand-400 hover:text-brand-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-300/60 sm:col-span-2"
                  data-q="1"
                  data-v="good"
                  aria-pressed="false"
                >
                  Gut - wir brauchen Umsetzung, keine Analyse
                </button>
              </div>
            </div>

            <div
              class="quiz-step hidden rounded-2xl border border-surface-200 bg-white p-6"
              data-step="2"
            >
              <p class="text-base font-medium text-slate-800">2. Was ist deine Priorität gerade?</p>
              <div class="mt-4 grid gap-3 sm:grid-cols-2">
                <button
                  type="button"
                  class="quiz-btn rounded-xl border border-slate-200 bg-white px-4 py-3 text-left text-sm font-medium text-slate-700 transition hover:border-brand-400 hover:text-brand-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-300/60"
                  data-q="2"
                  data-v="clarity"
                  aria-pressed="false"
                >
                  Klarheit &amp; Roadmap
                </button>
                <button
                  type="button"
                  class="quiz-btn rounded-xl border border-slate-200 bg-white px-4 py-3 text-left text-sm font-medium text-slate-700 transition hover:border-brand-400 hover:text-brand-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-300/60"
                  data-q="2"
                  data-v="features"
                  aria-pressed="false"
                >
                  Features &amp; Entwicklung
                </button>
                <button
                  type="button"
                  class="quiz-btn rounded-xl border border-slate-200 bg-white px-4 py-3 text-left text-sm font-medium text-slate-700 transition hover:border-brand-400 hover:text-brand-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-300/60 sm:col-span-2"
                  data-q="2"
                  data-v="endtoend"
                  aria-pressed="false"
                >
                  Komplett-Lösung von A-Z
                </button>
              </div>
            </div>

            <div
              class="quiz-step hidden rounded-2xl border border-surface-200 bg-white p-6"
              data-step="3"
            >
              <p class="text-base font-medium text-slate-800">3. Wie ist dein Team aufgestellt?</p>
              <div class="mt-4 grid gap-3 sm:grid-cols-2">
                <button
                  type="button"
                  class="quiz-btn rounded-xl border border-slate-200 bg-white px-4 py-3 text-left text-sm font-medium text-slate-700 transition hover:border-brand-400 hover:text-brand-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-300/60"
                  data-q="3"
                  data-v="none"
                  aria-pressed="false"
                >
                  Kein internes D365-Team
                </button>
                <button
                  type="button"
                  class="quiz-btn rounded-xl border border-slate-200 bg-white px-4 py-3 text-left text-sm font-medium text-slate-700 transition hover:border-brand-400 hover:text-brand-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-300/60"
                  data-q="3"
                  data-v="existing"
                  aria-pressed="false"
                >
                  Bestehendes Team, das Verstärkung braucht
                </button>
                <button
                  type="button"
                  class="quiz-btn rounded-xl border border-slate-200 bg-white px-4 py-3 text-left text-sm font-medium text-slate-700 transition hover:border-brand-400 hover:text-brand-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-300/60 sm:col-span-2"
                  data-q="3"
                  data-v="want-independent"
                  aria-pressed="false"
                >
                  Team, das unabhängig werden soll
                </button>
              </div>
            </div>
          </div>

          <div id="quiz-result" class="hidden" aria-live="polite">
            <div
              class="quiz-result-card space-y-5 rounded-2xl border-2 border-brand-200 bg-white p-6 shadow-soft"
            >
              <div class="flex items-center gap-2">
                <span
                  class="flex h-8 w-8 items-center justify-center rounded-full bg-brand-100 text-brand-600"
                >
                  <svg viewBox="0 0 20 20" fill="currentColor" class="h-5 w-5">
                    <path
                      fill-rule="evenodd"
                      d="M16.707 5.293a1 1 0 0 1 0 1.414l-7.25 7.25a1 1 0 0 1-1.414 0l-3.5-3.5a1 1 0 1 1 1.414-1.414l2.793 2.793 6.543-6.543a1 1 0 0 1 1.414 0z"
                      clip-rule="evenodd"></path>
                  </svg>
                </span>
                <p class="text-sm font-semibold uppercase tracking-wide text-brand-700">
                  Meine Empfehlung für dich
                </p>
              </div>
              <div>
                <span
                  id="quiz-result-badge"
                  class="mb-2 inline-flex items-center rounded-full bg-brand-50 px-3 py-1 text-xs font-semibold text-brand-700 ring-1 ring-brand-200/60"
                ></span>
                <h4 id="quiz-result-title" class="text-2xl font-semibold text-slate-900"></h4>
                <p id="quiz-result-tagline" class="mt-1 text-sm font-medium italic text-brand-600">
                </p>
              </div>
              <p id="quiz-result-text" class="text-base text-slate-600"></p>
              <ul id="quiz-answer-summary" class="grid gap-2 text-xs text-slate-500 sm:grid-cols-3">
              </ul>
              <div class="flex flex-wrap items-center gap-3 pt-1">
                <a
                  id="quiz-result-cal"
                  href="https://outlook.office.com/bookwithme/user/c7bd6282644a4fcb82fd8cc491a921e5@dynamics-tim.dev?anonymous&ismsaljsauthenabled&ep=plink"
                  target="_blank"
                  rel="noopener noreferrer"
                  class="cta-glow inline-flex items-center justify-center rounded-2xl bg-gradient-to-r from-signal-400 via-signal-300 to-signal-500 px-6 py-3 text-sm font-semibold text-brand-900 transition-all duration-300 hover:-translate-y-0.5 hover:shadow-xl"
                >
                  30-Min Slot buchen →
                </a>
                <a
                  id="quiz-result-link"
                  href="#engagement-modelle"
                  class="inline-flex items-center gap-1 text-sm font-semibold text-brand-500 hover:text-brand-400"
                >
                  Mehr zum Modell &rarr;
                </a>
                <button
                  id="quiz-edit"
                  type="button"
                  class="rounded-lg border border-surface-300 px-3 py-2 text-sm font-medium text-slate-600 transition hover:border-brand-300 hover:text-brand-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-300/60"
                >
                  Neu starten
                </button>
              </div>
              <p class="flex items-center gap-1.5 text-xs text-slate-400">
                <span class="inline-block h-2 w-2 rounded-full bg-green-400 animate-pulse"></span>
                Nächste verfügbare Kapazität: {site.availability.label}
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
  <script>
    (function () {
      const totalSteps = 3;
      const answers: Record<string, string> = {};
      const answerLabels: Record<string, string> = {};
      const questionLabels: Record<string, string> = {
        "1": "Zustand",
        "2": "Priorität",
        "3": "Team",
      };
      const btns = document.querySelectorAll<HTMLButtonElement>(".quiz-btn");
      const steps = Array.from(document.querySelectorAll<HTMLElement>(".quiz-step"));
      const resultEl = document.getElementById("quiz-result");
      const resultText = document.getElementById("quiz-result-text");
      const resultLink = document.getElementById("quiz-result-link") as HTMLAnchorElement | null;
      const stepLabel = document.getElementById("quiz-step-label");
      const progressBar = document.getElementById("quiz-progress-bar") as HTMLElement | null;
      const stepsWrapper = document.getElementById("quiz-steps");
      const answerSummary = document.getElementById("quiz-answer-summary");
      const resetTop = document.getElementById("quiz-reset-top") as HTMLButtonElement | null;
      const editBtn = document.getElementById("quiz-edit") as HTMLButtonElement | null;

      function getRecommendation() {
        const q1 = answers["1"],
          q2 = answers["2"],
          q3 = answers["3"];
        if (q1 === "unknown" || q2 === "clarity") {
          return {
            title: "Audit & Strategie",
            badge: "Schnelleinstieg · 1–2 Wochen",
            tagline: "Du bekommst Klarheit — nicht nur Folien.",
            text: "Ich analysiere deine aktuelle Dynamics 365 Landschaft und liefere dir einen priorisierten Aktionsplan mit Quick Wins und Roadmap — bevor du in Umsetzung investierst.",
            href: "#audit",
          };
        }
        if (q2 === "features" || q3 === "existing") {
          return {
            title: "Embedded Developer",
            badge: "Laufend · Sprint-basiert",
            tagline: "Senior-Kraft ins Team — ohne Overhead.",
            text: "Du bekommst sofort Senior-Kapazität in deinen Sprint-Prozess integriert — Features, Plugins, Integrationen und Tests im Sprint-Rhythmus.",
            href: "#embedded",
          };
        }
        return {
          title: "Full Package: Audit → Enablement",
          badge: "End-to-End · 3–4 Monate",
          tagline: "Einmal richtig machen. Dauerhaft wirken.",
          text: "Vom Audit über Umsetzung bis zur Team-Übergabe — dein Team kann am Ende eigenständig releasen und weiterentwickeln.",
          href: "#fullpackage",
        };
      }

      function answeredCount() {
        return Object.keys(answers).length;
      }

      function setActiveStep(step: number) {
        steps.forEach((stepEl) => {
          const isActive = Number(stepEl.dataset.step) === step;
          stepEl.classList.toggle("hidden", !isActive);
          stepEl.setAttribute("aria-hidden", isActive ? "false" : "true");
        });
      }

      function updateProgress() {
        const done = answeredCount();
        if (stepLabel) {
          stepLabel.textContent =
            done === totalSteps ? "Auswertung bereit" : `Frage ${done + 1} von ${totalSteps}`;
        }
        if (progressBar) {
          progressBar.style.width = `${(done / totalSteps) * 100}%`;
        }
        if (resetTop) {
          resetTop.classList.toggle("hidden", done === 0);
        }
      }

      function renderAnswerSummary() {
        if (!answerSummary) return;
        answerSummary.innerHTML = "";
        ["1", "2", "3"].forEach((question) => {
          const item = document.createElement("li");
          item.className = "rounded-xl border border-surface-200 bg-surface-50 px-3 py-2";
          item.textContent = `${questionLabels[question]}: ${answerLabels[question] ?? "n/a"}`;
          answerSummary.appendChild(item);
        });
      }

      function clearResultView() {
        if (!resultEl) return;
        resultEl.classList.add("hidden");
        if (stepsWrapper) {
          stepsWrapper.classList.remove("hidden");
        }
      }

      function revealResult() {
        if (!resultEl || !resultText) return;
        const rec = getRecommendation();
        const resultTitle = document.getElementById("quiz-result-title");
        const resultBadge = document.getElementById("quiz-result-badge");
        const resultTagline = document.getElementById("quiz-result-tagline");
        resultText.textContent = rec.text;
        if (resultTitle) resultTitle.textContent = rec.title;
        if (resultBadge) resultBadge.textContent = rec.badge;
        if (resultTagline) resultTagline.textContent = '"' + rec.tagline + '"';
        if (resultLink) resultLink.href = rec.href;
        renderAnswerSummary();
        resultEl.classList.remove("hidden");
        if (stepsWrapper) {
          stepsWrapper.classList.add("hidden");
        }
        if (stepLabel) {
          stepLabel.textContent = "Empfehlung";
        }
        if (progressBar) {
          progressBar.style.width = "100%";
        }
        const reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
        const card = resultEl.querySelector(".quiz-result-card") as HTMLElement | null;
        if (card && !reducedMotion) {
          card.style.opacity = "0";
          card.style.transform = "translateY(20px)";
          requestAnimationFrame(() => {
            requestAnimationFrame(() => {
              card.style.transition =
                "opacity 0.5s cubic-bezier(0.16, 1, 0.3, 1), transform 0.5s cubic-bezier(0.16, 1, 0.3, 1)";
              card.style.opacity = "1";
              card.style.transform = "translateY(0)";
            });
          });
        }
        resultEl.scrollIntoView({ behavior: reducedMotion ? "auto" : "smooth", block: "nearest" });
      }

      function updateSelectedState(question: string, activeButton: HTMLButtonElement) {
        document
          .querySelectorAll<HTMLButtonElement>(`.quiz-btn[data-q="${question}"]`)
          .forEach((btn) => {
            const isActive = btn === activeButton;
            btn.classList.toggle("border-brand-400", isActive);
            btn.classList.toggle("bg-brand-50", isActive);
            btn.classList.toggle("text-brand-700", isActive);
            btn.classList.toggle("shadow-sm", isActive);
            btn.setAttribute("aria-pressed", isActive ? "true" : "false");
          });
      }

      function resetQuiz() {
        Object.keys(answers).forEach((key) => {
          delete answers[key];
        });
        Object.keys(answerLabels).forEach((key) => {
          delete answerLabels[key];
        });
        btns.forEach((btn) => {
          btn.classList.remove("border-brand-400", "bg-brand-50", "text-brand-700", "shadow-sm");
          btn.setAttribute("aria-pressed", "false");
        });
        clearResultView();
        setActiveStep(1);
        updateProgress();
      }

      btns.forEach((btn) => {
        btn.addEventListener("click", () => {
          const q = btn.dataset.q!;
          const v = btn.dataset.v!;
          answers[q] = v;
          answerLabels[q] = btn.textContent?.trim() ?? "";
          updateSelectedState(q, btn);
          updateProgress();
          if (answeredCount() === totalSteps) {
            revealResult();
          } else {
            clearResultView();
            setActiveStep(Math.min(Number(q) + 1, totalSteps));
          }
        });
      });

      if (resetTop) {
        resetTop.addEventListener("click", () => {
          resetQuiz();
        });
      }

      if (editBtn) {
        editBtn.addEventListener("click", () => {
          resetQuiz();
            const reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
            document.getElementById("quiz-container")?.scrollIntoView({
              behavior: reducedMotion ? "auto" : "smooth",
              block: "nearest",
            });
        });
      }

      setActiveStep(1);
      updateProgress();
    })();
  </script>

  <section class="bg-white py-20 animate-on-scroll" id="referenzen">
    <div class="mx-auto max-w-6xl px-6">
      <h2 class="section-title" data-reveal data-reveal-delay="0">Stimmen aus der Praxis</h2>
      <p class="mt-4 max-w-3xl text-lg text-slate-600" data-reveal data-reveal-delay="80">
        Entscheidungen werden leichter, wenn andere den ersten Schritt schon gemacht haben.
      </p>
      <div class="mt-10 grid gap-6 md:grid-cols-3" data-reveal data-reveal-delay="160">
        <div class="card flex flex-col gap-4 bg-white/90" data-reveal data-reveal-delay="200">
          <div class="text-3xl text-brand-300 font-serif leading-none">"</div>
          <p class="text-base text-slate-700 flex-1">
            Tim überzeugte durch seine exzellente Analysefähigkeit und fand für alle auftretenden
            Probleme ausnahmslos gute Lösungen.
          </p>
          <div class="border-t border-surface-100 pt-3">
            <p class="text-sm font-semibold text-slate-900">Microsoft Platform Solutions</p>
            <p class="text-xs text-slate-500 mt-0.5">ORBIS SE</p>
          </div>
        </div>
        <div
          class="card flex flex-col gap-4 bg-brand-900/95 text-white"
          data-reveal
          data-reveal-delay="280"
        >
          <div class="grid grid-cols-3 gap-4 text-center">
            <div>
              <p class="text-2xl font-bold text-signal-400" data-counter>20+</p>
              <p class="text-xs text-white/70 mt-0.5">D365 Projekte</p>
            </div>
            <div>
              <p class="text-2xl font-bold text-signal-400" data-counter>5+</p>
              <p class="text-xs text-white/70 mt-0.5">Zertifizierungen</p>
            </div>
            <div>
              <p class="text-2xl font-bold text-signal-400" data-counter>Ø 3</p>
              <p class="text-xs text-white/70 mt-0.5">Mon. Audit→Go-Live</p>
            </div>
          </div>
          <div class="border-t border-white/10 pt-4 space-y-2">
            <p class="text-xs font-semibold uppercase tracking-wide text-white/50">
              Fokus-Industrien
            </p>
            <div class="flex flex-wrap gap-2">
              {
                ["Maschinenbau", "IT-Services", "Handel"].map((ind) => (
                  <span class="rounded-full bg-white/10 px-2.5 py-0.5 text-xs text-white/80">
                    {ind}
                  </span>
                ))
              }
            </div>
          </div>
        </div>
        <div class="card flex flex-col gap-4 bg-white/90" data-reveal data-reveal-delay="360">
          <div class="text-3xl text-brand-300 font-serif leading-none">"</div>
          <p class="text-base text-slate-700 flex-1">
            Saubere Architektur, klare Kommunikation und ein Team, das nach dem Projekt selbst
            weiterarbeiten kann — genau das haben wir bekommen.
          </p>
          <div class="border-t border-surface-100 pt-3">
            <p class="text-sm font-semibold text-slate-900">M. Schneider, Projektleitung</p>
            <p class="text-xs text-slate-500 mt-0.5">Fertigungsunternehmen, Süddeutschland</p>
            <p class="text-xs text-slate-400 mt-1 flex items-center gap-1">
              <svg viewBox="0 0 20 20" fill="currentColor" class="h-3 w-3 text-brand-400"
                ><path
                  fill-rule="evenodd"
                  d="M16.707 5.293a1 1 0 0 1 0 1.414l-7.25 7.25a1 1 0 0 1-1.414 0l-3.5-3.5a1 1 0 1 1 1.414-1.414l2.793 2.793 6.543-6.543a1 1 0 0 1 1.414 0z"
                  clip-rule="evenodd"></path></svg
              >
              Verifizierte Referenz
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="bg-surface-50 py-20 animate-on-scroll">
    <div class="mx-auto max-w-6xl px-6">
      <h2 class="section-title" data-reveal data-reveal-delay="0">
        So läuft eine Zusammenarbeit ab
      </h2>
      <p class="mt-4 max-w-3xl text-lg text-slate-600" data-reveal data-reveal-delay="80">
        Kein generisches Consulting-Framework. Ein erprobter Ablauf mit klarer Meinung — damit du
        weißt, was dich erwartet.
      </p>
      <div
        class="mt-12 grid gap-6 md:grid-cols-2 lg:grid-cols-4"
        data-reveal
        data-reveal-delay="160"
      >
        {
          approachSteps.map((step, index) => (
            <div
              class="card relative overflow-hidden"
              data-reveal
              data-reveal-delay={`${index * 80 + 200}`}
            >
              <div
                class="absolute top-0 right-0 text-[5rem] font-black leading-none text-surface-200 select-none pointer-events-none"
                aria-hidden="true"
              >
                {step.phase}
              </div>
              <span class="inline-flex items-center rounded-full bg-brand-50 px-2.5 py-0.5 text-xs font-medium text-brand-700 ring-1 ring-brand-200/60 mb-3">
                {step.duration}
              </span>
              <h3 class="text-xl font-semibold text-slate-900">{step.title}</h3>
              <p class="mt-1 text-sm font-medium text-brand-600 italic">"{step.tagline}"</p>
              <p class="mt-3 text-sm text-slate-600">{step.description}</p>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <section class="bg-surface-100 py-20 animate-on-scroll" id="faq">
    <div class="mx-auto max-w-6xl px-6">
      <h2 class="section-title" data-reveal data-reveal-delay="0">
        FAQ: Dynamics 365 Zusammenarbeit
      </h2>
      <div class="mt-10 space-y-6" data-reveal data-reveal-delay="80">
        {
          faqs.map((faq, index) => (
            <details
              class="group rounded-2xl border border-surface-200 bg-white/90 p-6 shadow-soft transition"
              data-reveal
              data-reveal-delay={`${index * 80 + 160}`}
            >
              <summary class="flex cursor-pointer items-center justify-between gap-4 text-left text-lg font-semibold text-slate-900">
                <span>{faq.question}</span>
                <span class="text-brand-400 transition group-open:rotate-45" aria-hidden="true">
                  +
                </span>
              </summary>
              <div class="mt-4 text-base text-slate-600" set:html={faq.answerHtml} />
            </details>
          ))
        }
      </div>
    </div>
  </section>

  <section
    class="bg-gradient-to-br from-brand-900 via-brand-800 to-accent-600 py-24 text-white"
    data-reveal
    data-reveal-delay="0"
  >
    <div class="mx-auto max-w-5xl px-6 text-center">
      <h2 class="text-3xl font-bold sm:text-4xl" data-reveal data-reveal-delay="0">
        Bereit für Verstärkung in deinem Dynamics 365 Team?
      </h2>
      <p class="mt-4 text-lg text-slate-200" data-reveal data-reveal-delay="80">
        Lass uns im kostenlosen Erstgespräch klären, wie ich dein Team mit Senior-Level Expertise in
        Development und Technical Consulting unterstützen kann.
      </p>
      <div class="mt-8 flex flex-wrap justify-center gap-4" data-reveal data-reveal-delay="160">
        <a
          href="https://outlook.office.com/bookwithme/user/c7bd6282644a4fcb82fd8cc491a921e5@dynamics-tim.dev?anonymous&ismsaljsauthenabled&ep=plink"
          target="_blank"
          rel="noopener noreferrer"
          class="cta-glow rounded-2xl bg-white px-6 py-3 text-base font-semibold text-brand-700 shadow-soft transition hover:bg-surface-200 btn-pulse"
        >
          30-Min Slot buchen →
        </a>
        <a
          href={to("services")}
          class="cta-outline rounded-2xl border border-white/60 px-6 py-3 text-base font-semibold text-white transition hover:bg-white/15"
        >
          Dynamics 365 Leistungen ansehen
        </a>
      </div>
      <p
        class="mt-4 text-sm text-white/50 flex items-center justify-center gap-2"
        data-reveal
        data-reveal-delay="200"
      >
        <span class="inline-block h-2 w-2 rounded-full bg-green-400 animate-pulse"></span>
        Nächste verfügbare Kapazität: {site.availability.label}
      </p>
    </div>
  </section>
  {/* Scroll-depth Quick-Audit banner */}
  <div
    id="quick-audit-banner"
    class="fixed bottom-0 left-0 right-0 z-50 hidden translate-y-full transform transition-transform duration-500 ease-out"
    role="complementary"
    aria-label="Quick Audit Angebot"
  >
    <div class="border-t border-brand-200 bg-brand-50 shadow-[0_-4px_30px_rgba(0,39,76,0.12)]">
      <div class="mx-auto flex max-w-5xl items-center justify-between gap-4 px-6 py-4 flex-wrap">
        <div class="flex items-start gap-3">
          <span
            class="mt-0.5 flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-brand-100 text-brand-700 text-base"
            >⚡</span
          >
          <div>
            <p class="text-sm font-semibold text-slate-900">
              Unsicher, ob dein CRM richtig aufgestellt ist?
            </p>
            <p class="text-sm text-slate-600">
              Hol dir meinen <strong>10-Punkte Dynamics 365 Quick-Check</strong> — kostenlos per Mail.
            </p>
          </div>
        </div>
        <div class="flex items-center gap-3 shrink-0">
          <a
            href="mailto:hello@dynamics-tim.dev?subject=Quick-Check%20anfragen&body=Hallo%20Tim%2C%20ich%20m%C3%B6chte%20den%20kostenlosen%20Dynamics%20365%20Quick-Check%20erhalten."
            class="rounded-xl bg-brand-600 px-4 py-2 text-sm font-semibold text-white shadow-sm transition hover:bg-brand-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-500"
          >
            Quick-Check anfordern
          </a>
          <button
            id="quick-audit-close"
            class="rounded-lg p-1.5 text-slate-400 transition hover:bg-brand-100 hover:text-slate-600 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-500"
            aria-label="Schließen"
          >
            <svg viewBox="0 0 20 20" fill="currentColor" class="h-5 w-5" aria-hidden="true">
              <path
                d="M6.28 5.22a.75.75 0 0 0-1.06 1.06L8.94 10l-3.72 3.72a.75.75 0 1 0 1.06 1.06L10 11.06l3.72 3.72a.75.75 0 1 0 1.06-1.06L11.06 10l3.72-3.72a.75.75 0 0 0-1.06-1.06L10 8.94 6.28 5.22Z"
              ></path>
            </svg>
          </button>
        </div>
      </div>
    </div>
  </div>

  <script>
    (function () {
      const banner = document.getElementById("quick-audit-banner");
      const closeBtn = document.getElementById("quick-audit-close");
      if (!banner || !closeBtn) return;

      const STORAGE_KEY = "qa-banner-dismissed";
      let shown = false;

      function showBanner() {
        if (shown || sessionStorage.getItem(STORAGE_KEY) || !banner) return;
        shown = true;
        banner.classList.remove("hidden");
        requestAnimationFrame(() => {
          requestAnimationFrame(() => {
            banner!.classList.remove("translate-y-full");
          });
        });
      }

      function hideBanner() {
        if (!banner) return;
        banner.classList.add("translate-y-full");
        setTimeout(() => banner!.classList.add("hidden"), 500);
        sessionStorage.setItem(STORAGE_KEY, "1");
      }

      closeBtn.addEventListener("click", hideBanner);

      // Trigger on 60% scroll depth
      function onScroll() {
        const scrolled = window.scrollY + window.innerHeight;
        const total = document.documentElement.scrollHeight;
        if (scrolled / total >= 0.6) {
          showBanner();
          window.removeEventListener("scroll", onScroll);
        }
      }
      window.addEventListener("scroll", onScroll, { passive: true });

      // Also trigger on mouseleave (desktop exit intent)
      document.documentElement.addEventListener("mouseleave", (e) => {
        if ((e as MouseEvent).clientY <= 0) showBanner();
      });

      // Quick-Check form submission
      const auditForm = document.getElementById("quick-audit-form") as HTMLFormElement | null;
      const auditEmail = document.getElementById("quick-audit-email") as HTMLInputElement | null;
      const auditSuccess = document.getElementById("quick-audit-success");
      const auditError = document.getElementById("quick-audit-error");

      if (auditForm && auditEmail) {
        auditForm.addEventListener("submit", async (e) => {
          e.preventDefault();
          const email = auditEmail.value.trim();
          if (!email || !auditEmail.checkValidity()) {
            auditEmail.focus();
            return;
          }

          const submitBtn = auditForm.querySelector('button[type="submit"]') as HTMLButtonElement;
          if (submitBtn) {
            submitBtn.disabled = true;
            submitBtn.textContent = "Sende\u2026";
          }

          try {
            const res = await fetch("https://n8n.dynamics-tim.dev/webhook/quick-check", {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                email,
                source: window.location.href,
                timestamp: new Date().toISOString(),
              }),
            });

            if (res.ok) {
              if (auditSuccess) auditSuccess.classList.remove("hidden");
              if (auditError) auditError.classList.add("hidden");
              auditEmail.value = "";
              auditEmail.disabled = true;
              if (submitBtn) submitBtn.style.display = "none";
              setTimeout(() => hideBanner(), 3000);
            } else {
              throw new Error("Request failed");
            }
          } catch {
            if (auditError) auditError.classList.remove("hidden");
            if (auditSuccess) auditSuccess.classList.add("hidden");
            if (submitBtn) {
              submitBtn.disabled = false;
              submitBtn.textContent = "Quick-Check anfordern";
            }
          }
        });
      }
    })();
  </script>

  <script type="application/ld+json" set:html={JSON.stringify(structuredData)} />
</BaseLayout>
