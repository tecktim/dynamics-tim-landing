---
import BaseLayout from '../layouts/BaseLayout.astro';

const base = import.meta.env.BASE_URL ?? '/';
---

<BaseLayout title="Seite nicht gefunden" showHeader={false} showFooter={false}>
  <section class="flex min-h-screen flex-col items-center justify-center bg-gradient-to-br from-brand-900 via-brand-800 to-accent-600 px-6 text-center text-white">
    <p class="text-sm uppercase tracking-[0.3em] text-brand-300">404</p>
    <h1 class="mt-4 text-4xl font-semibold sm:text-5xl">Ups, hier gibt es nichts.</h1>
    <p class="mt-4 max-w-xl text-base text-slate-200">
      Die gesuchte Seite existiert nicht oder wurde verschoben. ZurÃ¼ck zur Startseite und gemeinsam Dynamics 365 beschleunigen.
    </p>
    <a href={base} class="mt-8 rounded-2xl bg-white px-6 py-3 text-base font-semibold text-brand-700 shadow-soft transition hover:bg-surface-200">
      Zur Startseite
    </a>
  </section>
</BaseLayout>

